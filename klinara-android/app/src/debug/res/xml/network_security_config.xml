<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
      10.0.2.2  = emülatörden host loopback'i.
      localhost / 127.0.0.1 = fiziksel cihaz, `adb reverse tcp:3000 tcp:3000` ile.

      LAN IP girişi BİLEREK yok: LAN adresi Gradle'dan resource XML'e şablonlanamaz ve
      wildcard açmak "tek yanlış yapılandırma tüm trafiği düz metne düşürür" hatasıdır (§5.4).
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
