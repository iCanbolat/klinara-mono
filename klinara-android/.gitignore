.gradle/
build/
local.properties
.kotlin/
.cxx/
captures/
*.iml
.idea/

# İmza materyali — asla commit edilmez (P2)
*.jks
*.keystore

# Çıktılar
*.apk
*.aab
# `release/` DEĞİL: o desen `app/src/release/` KAYNAK kümesini de yutuyordu ve
# release varyantının kökü (`RootContent.kt`) hiç sürüm kontrolüne girmemişti —
# temiz bir klonda release derlemesi kırılırdı. Yalnız derleme çıktısı hariç tutulur.
app/build/outputs/
lint-results*.html
lint-results*.xml
lint-results*.txt
